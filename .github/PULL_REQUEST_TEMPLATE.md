## What changed

## Verification

- [ ] `pnpm typecheck && pnpm test` green against a real Postgres
- [ ] If this touched retrieval: eval numbers reported, not just tests passing
- [ ] If this touched `DESIGN.md`'s rules: the file was updated, not worked around
